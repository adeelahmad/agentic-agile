# transcript store + per-worktree control dir (never tracked, never merged)
.agentic/
.transcripts/
# rust build
target/
# generated eval runs (only the evals.json suites are tracked)
eval-workspace/
# release bundles built by `make package`
package/
