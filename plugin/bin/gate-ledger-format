#!/usr/bin/env bash
# execution.log: each line matches the fixed transition format.
set -euo pipefail
f="${1:?execution.log}"
grep -vqE '^[0-9]{4}-[0-9]{2}-[0-9]{2}T.* (wave-[0-9]+) (red|green|scaffold|review|gate) ' "$f" \
  && { echo "bad ledger line" >&2; exit 2; } || exit 0
